---
'@metatell/bot-sdk': patch
'@metatell/bot-core': patch
'@metatell/bot-realtime': patch
'@metatell/bot-cli': patch
---

Refresh package READMEs to match the current public API: `authToken` and avatar options, the actual error classes, `enableVoice()` audio format and `detach()`, scene navigation limits and abort behavior, the realtime transport factory, core service composition, and CLI commands and options.
