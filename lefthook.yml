pre-commit:
  parallel: true
  jobs:
    - glob: "*.{js,mjs,ts,mts,jsx,tsx,json,jsonc}"
      run: pnpm check -- {staged_files}

pre-push:
  parallel: true
  jobs:
    - glob: "*.{js,mjs,ts,mts,jsx,tsx,json,jsonc}"
      run: pnpm check -- {push_files}

