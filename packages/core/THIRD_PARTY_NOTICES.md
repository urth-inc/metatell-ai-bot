# Third-party notices

The navigation preparation feature uses the following third-party packages:

- `@gltf-transform/core` and `@gltf-transform/extensions` 4.4.1 — MIT
- `meshoptimizer` 1.2.0 — MIT
- `draco3dgltf` 1.5.7 — Apache-2.0

The full license texts are distributed by each dependency package.
