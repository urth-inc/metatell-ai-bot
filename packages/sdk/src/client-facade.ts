/**
 * Re-export MetatellClient from core
 * SDK provides a facade to the core implementation
 */
export {
  type CreateClientOptions,
  createMetatellClient,
  type MetatellClient,
  type MetatellClientEvents,
} from '@metatell/bot-core'
