# Third-party notices

The navigation runtime uses the following third-party packages:

- `three` 0.185.1 — MIT
- `three-pathfinding` 1.3.0 — MIT

GLB navigation preparation is provided by `@metatell/bot-core`; see that package's third-party notices for its parser and decoder dependencies.

The full license texts are distributed by each dependency package.
